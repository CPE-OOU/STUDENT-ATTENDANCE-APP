'use client';

interface StudentClientProps {}

const StudentsDataTable: React.FC<StudentClientProps> = ({}) => {
  return (
    <div className="flex justify-between items-center">
      <h2>Student Records</h2>
    </div>
  );
};
